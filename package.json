{
  "name": "spotifyoverlay",
  "version": "0.1.0",
  "description": "Always-on-top lyrics overlay for Spotify/YouTube playback on Windows 11",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "lint": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js"
  },
  "dependencies": {
    "electron-store": "^10.0.1"
  },
  "devDependencies": {
    "electron": "^36.3.2"
  }
}
